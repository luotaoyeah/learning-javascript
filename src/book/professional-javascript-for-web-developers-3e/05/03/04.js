// the-date-type

'use strict';

(function () {
  // ----------------------------------------------------------------------------------------------------
  // `Date.now()` 方法, 返回当前时间的毫秒数

  console.assert(Date.now() === new Date().getTime());
})();
