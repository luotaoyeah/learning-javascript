/*
 * 将 array 或者 array-like object 的元素连接起来；
 */

/*
 * 如果没有指定 separator, 默认为（,）；
 */
console.log('\n-------------------------------------------------- 01');
/* 1,2,3 */
console.log([1, 2, 3].join());

export {};
