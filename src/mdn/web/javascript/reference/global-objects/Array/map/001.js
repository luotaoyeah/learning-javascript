/*
 * 创建一个新的数组, 元素为对原数组的每一个元素分别调用函数, 返回的结果;
 */

console.log('\n-------------------------------------------------- 01');
/* [ 1, 4, 9 ] */
console.log([1, 2, 3].map((value) => value * value));

export {};
