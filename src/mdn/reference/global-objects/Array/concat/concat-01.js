/*
 * 如果参数是 Array, 则合并其元素;
 * 如果参数不是 Array, 则合并参数本身;
 */

console.log("\n-------------------------------------------------- 01");
/* [ 1, 2, 3, 4, 5 ] */
console.log([1, 2, 3].concat([4], 5));

export {};
