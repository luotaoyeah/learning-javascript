/*
 * 13.4 Compatability and Interoperability
 */

(function() {
  /*
   * 客户端 JS 的兼容性主要存在 3 个方面的问题：
   *     1. 新的 web 特性和 API 只在较新的浏览器实现，
   *        老的浏览器不支持这些新的特性；
   *     2. 对于有的特性，一些浏览器实现了，一些浏览器没有实现；
   *     3. 浏览器存在 BUG；
   */
  console.log("\n-------------------------------------------------- 01");
})();
