/*
 * 13.7 Client-Side Frameworks
 */

(function() {
  /*
   * JS 框架在底层 API 的基础上，提供了更高层次的 API，
   * 同时带来的好处是：
   *     a. 处理不同浏览器之间的兼容性问题
   *     b. 处理安全问题
   *     c. 处理可访问性问题
   *     d. 使用更少的代码实现相同的功能
   *
   * 因为框架是基于底层原生的 API 构建的，
   * 因此掌握底层原生 API 对于更好的使用和理解框架非常重要；
   */
  console.log("\n-------------------------------------------------- 01");
})();
