/*
 * Promise
 *     Promises Overview
 */

console.log('\n-------------------------------------------------- 01');
/*
 * promise 对象有三种状态：pending, fulfilled, rejected
 * 可以由 pending 变更为 fulfilled
 * 也可以由 pending 变更为 rejected
 *
 * 当状态为 fulfilled 或者 rejected 时, 称之为 settled
 * 此时状态不能再变更
 *
 * fulfilled 表示成功
 * rejected 表示失败
 */

export {};
