/*
 * Getting Started with ES6
 *     One JavaScript
 */

/*
 * JS 的升级策略不像其他语言, 比如 python 语言,
 * 从 py2 升级到 py3 之后, 以前的很多代码就不再兼容,
 * 要想使用新版本, 就必须迁移旧代码；
 * 而 JS 遵循完全的向后兼容性, 所有的 ES5 代码都是合法的 ES6 代码,
 * 只是在旧版本的基础上添加更多的语言特性, 而不会移除旧的语言特性；
 */
